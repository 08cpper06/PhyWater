# Setup
This program depends on ```SDL```.
execute ```clone_SDL.sh``` brefore building.

# How to build
```
mkdir build
cd build
cmake ..
make -j`nproc`
```

# How to run
```
cd build
./phy_water
```
